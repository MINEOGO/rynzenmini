# RynZen Mini

a shitty copy of rynzen you can run locally, this supports:
- linux
- android (app will be made soon, use termux for now ok?)
- microslop windows

hi this slop is a bit vibecoded. yes it contains actual sourcecode from rynzen, but its logic has been modified because im NOT leakijg my shit !!! rynzen is the FASTEST raid bot on dc, its not like any other raid bots, it uses different logics and functions which i cant afford to leak.

# this is slower than rynzen !!!!
why?: because i dont want krownskids and kryptex skidding my code, and make THEIR bots better. if you want real fast speed, use the REAL rynzen from https://discord.gg/aqGWKUFxCw .

## Installation Setup

Choose the command matching your operating system to quickly download and configure the bot:

### Android (Termux)
```bash
curl -sSL https://raw.githubusercontent.com/MINEOGO/rynzenmini/main/setup_termux.sh | bash
```

### Debian / Ubuntu
```bash
curl -sSL https://raw.githubusercontent.com/MINEOGO/rynzenmini/main/setup_deb.sh | bash
```

### Arch Linux
```bash
curl -sSL https://raw.githubusercontent.com/MINEOGO/rynzenmini/main/setup_archlinux.sh | bash
```

### Windows (PowerShell)
```powershell
irm https://raw.githubusercontent.com/MINEOGO/rynzenmini/main/setup_powershell.ps1 | iex
```
